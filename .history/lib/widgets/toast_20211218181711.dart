import 'package:flutter/material.dart';

class ToastNo extends StatelessWidget {
  const ToastNo({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}