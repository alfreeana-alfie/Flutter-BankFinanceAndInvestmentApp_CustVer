class Request {
  int? id;
  String? currencyId,
      amount,
      status,
      description,
      senderId,
      receiverId,
      transactionId,
      branchId;

  Request({this.id, this.currencyId, this.amount, this.status, this.description, this.senderId, this.re});
}
